import React, { useState } from 'react';
import { FiX, FiTarget, FiTrendingUp } from 'react-icons/fi';

const SetGoalModal = ({ isOpen, onClose, onGoalSet, currentSavings = 0 }) => {
    // ... (all the existing code remains the same)
};

// Make sure this is the default export
export default SetGoalModal;